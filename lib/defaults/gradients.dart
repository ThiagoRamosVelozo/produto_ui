import 'package:flutter/material.dart';

const gradient0 = LinearGradient(
  begin: Alignment.topCenter,
  end: Alignment.bottomCenter,
  colors: [
    Colors.blue,
    Color.fromARGB(255, 70, 70, 190),
  ],
);

const gradient1 = LinearGradient(
  begin: Alignment.topCenter,
  end: Alignment.bottomCenter,
  colors: [
    Color.fromARGB(255, 62, 216, 67),
    Color.fromARGB(255, 40, 157, 40),
  ],
);

const gradient2 = LinearGradient(
  begin: Alignment.topCenter,
  end: Alignment.bottomCenter,
  colors: [
    Color.fromARGB(255, 226, 43, 43),
    Color.fromARGB(255, 163, 19, 19),
  ],
);

const gradient3 = LinearGradient(
  begin: Alignment.topCenter,
  end: Alignment.bottomCenter,
  colors: [
    Colors.indigo,
    Color.fromARGB(255, 38, 49, 114),
  ],
);

const gradient4 = LinearGradient(
  begin: Alignment.topCenter,
  end: Alignment.bottomCenter,
  colors: [
    Colors.grey,
    Color.fromARGB(255, 61, 61, 61),
  ],
);
